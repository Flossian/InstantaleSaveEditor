# Instantale Save Editor（非公式）

バージョン: v10.3

ゲーム Instantale のセーブデータ／ワールドデータを項目ごとに編集するための非公式デスクトップツールです（C# / WinForms / .NET 8）。データを編集し、ゲームがそのまま読み込める形式で保存します。

> この公開は Instantale の作者様の許諾を得て行っています。作者様より取り下げのご要望があった場合は、速やかに公開を停止します。

## 注意・免責
- 本ツールは非公式です。Instantale の公式ではありません。
- 使用はすべて自己責任でお願いします。本ツールの利用によって生じたいかなる損害についても、作者は責任を負いません（MIT ライセンス無保証条項のとおり）。
- セーブデータを破損させる可能性があります。**編集前に必ずバックアップ**を取ってください。
- 本リポジトリには、Instantale 本体のデータ・アセット・セーブファイルは含まれていません。公開しているのは編集ツールのコードのみです。

## 主な機能

※ プレイヤー／NPC の編集系（インベントリ・スキル・生涯ログ・顔画像／立ち絵）は共通操作です。

- セーブ／ワールドデータの読み込み・編集・保存（ゲームが読める形式で書き戻し）
- ワールド生成（名前・シード・集落数を指定して新しいワールドを手続き生成。シードで再現可能、`namelist` フォルダの JSON で名前プールをカスタマイズ可）
- プレイヤー編集（基本値・能力値・身体部位・特性・スキル・インベントリ・装備・生涯ログ・エリア履歴・顔画像／立ち絵 など）
- インベントリのグリッド編集（ゲームと同じ格子に配置。ドラッグで移動／入れ替え、追加・編集・削除、画像表示）
- スキルの編集（名前・属性・種別・使用回数・有用度・説明・効果を構造化フォームで編集）
- ワールド編集（areas / npcs / quests / story_quests / index。ダンジョンは別ツリー、npcs / quests / dungeon は拠点ごとにグループ化。ツリー右クリックにレコード操作を集約＝新規作成・インポート・エクスポート・JSON直接編集・複製・削除）
- キャラクター画像（顔画像・立ち絵の表示／差し替え・顔切り抜き・外部画像の取込み）、facility 選択時の背景画像表示
- ゲーム変数の編集（カテゴリ別の折りたたみ表示。party は GUI で追加／削除）
- クエスト作成（テンプレ＋部品ライブラリから合成。ダンジョン追加と連動）と、既存データからの「クエストをテンプレ化」
- NPC のエクスポート／インポート（JSON＋画像を zip 化して別ワールドへ移植。`npc\{ワールド名}\` でワールド別整理）、プレイヤーを NPC 化
- 施設（facility）のエクスポート／インポート（facility JSON＋背景画像を zip 化して別ワールドへ移植。取込時に選んだ接続先施設と双方向に接続。`facility\{ワールド名}\` で整理）
- 自由施設（`facility_type` が `free` の施設）のプログラム編集（`free_facility_programs` の命令列を行ごとに追加・複製・削除・並べ替え、料金／報酬の編集。ツリー右クリックからプログラムの作成・編集）
- quest_event_log の圧縮（ゲーム側の既知バグでログが際限なく肥大化する問題への緩和策。最新数ターンのみ残して古い内容を削除）
- プレイヤーをプリセット化（`player_data` をゲームのキャラ作成画面で選べるキャラクタープリセットとして出力）
- アイテムのエクスポート／インポート（1 アイテムを zip 化して別キャラ・別セーブへ）と、アイテム倉庫（個別倉庫＋共有倉庫の 2 系統。プレイヤーのみ）
- ワールドデータのクリーンアップ（ストーリー進行を未完了に戻し、全 NPC のログ消去・関係を初期化）
- 多言語対応（日本語／英語／簡体字中国語。`lang` フォルダに JSON 追加で拡張可）、上書き保存時の自動バックアップ（zip 退避・世代自動削除は任意）
- 設定（バックアップ・言語・ウィンドウサイズ・ゲーム導入先・インベントリ格子サイズ・NPC エクスポートのワールド別出力・NPC 画像の読み込みスキップ）

## 動作環境
- Windows
- 単体実行ファイル（.NET ランタイム同梱）としてビルド可能

## 使い方（概要）

詳しい操作は [説明書（manual.html）](./manual.html) を参照してください。

1. 「ファイル → 開く」で対象データを選択
2. 各タブ（プレイヤー／ワールド／ゲーム変数）で編集（値はフォーカスが外れた時点で反映）
3. 「ファイル → 上書き保存」で保存（既定で旧ファイルを自動バックアップ）
4. クエスト追加は「ツール → クエスト作成（テンプレから）」から
5. UI 言語の切替・バックアップ・ウィンドウサイズは「設定」メニューから

## 更新履歴

v1〜v6（要約）
- v1: 初版。セーブ／ワールドデータの読み込み・編集・保存
- v2〜v3: 生涯ログのグリッド編集化、顔画像・立ち絵の表示／差し替え、ダンジョンの別ツリー表示、背景画像表示、説明書追加
- v4: 未生成NPCの編集ロック、好感度のグリッド表示化
- v5: エリア履歴のグリッド編集、ゲーム変数のカテゴリ別折りたたみ、party の GUI 編集、死亡キャラ「（死亡）」表示
- v6: クエスト作成の正式機能化、「クエストをテンプレ化」追加、ワールドツリーを拠点ごとにグループ化

v7
- 多言語対応を追加（日本語／英語。`lang` フォルダに JSON 追加で拡張可）
- NPC のエクスポート／インポートを追加（zip で別ワールドへ移植）
- プレイヤーを NPC 化を追加
- 自動バックアップと設定メニューを追加

v8
- インベントリのグリッド編集を追加（ドラッグで移動／入れ替え、追加・編集・削除、画像表示）
- スキル編集を追加（構造化フォームで編集）
- NPC 立ち絵に外部画像の取込み枠を追加
- 設定にゲーム導入先とインベントリ格子サイズを追加

v9
- アイテム倉庫を追加（個別倉庫＋共有倉庫の 2 系統）
- アイテムのエクスポート／インポートを追加（zip で別キャラ・別セーブへ）
- ワールドデータのクリーンアップを追加
- NPC のエクスポート／インポートを改修（`npc\{ワールド名}\` でワールド別整理、ワールド選択プルダウン）
- 別 NPC を開くと編集内容が破棄される不具合を修正

v9.1
- 未生成 NPC を編集するとゲームが起動しなくなる場合がある問題を修正（v4 の編集ロックは廃止）
- 保存時のデータ変換処理にある潜在的なバグを修正

v9.2
- キーボードショートカット（Ctrl+O／Ctrl+S／Ctrl+Shift+S）、ドラッグ＆ドロップで開く、最近開いたファイルを追加
- 未保存の変更の確認を追加、タイトルバーにファイル名表示、未読み込み時のメニュー無効化
- 画面が意図せずスクロールする問題を修正、保存完了ダイアログを廃止（ステータスバー表示に）

v10
- ワールド生成を追加（名前・シード・集落数から新しいワールドを生成。シードで再現可能、`namelist` フォルダで名前プールをカスタマイズ可）
- プレイヤーをプリセット化を追加（プレイヤーをゲームのキャラ作成画面で選べるプリセットとして出力）
- JSON 直接編集を追加（プレイヤータブ・ワールドタブで対象を生 JSON のまま編集可能）
- 簡体字中国語（简体中文）の UI を追加
- 意図せずプルダウンが操作されてしまう問題を修正
- タイトルバーにワールド名を表示
- 設定ファイルを `setting\` フォルダへ移動（旧ファイルは自動移行）

v10.1
- 施設（facility）のエクスポート／インポートを追加（facility JSON＋背景画像を zip 化。取込時に選んだ接続先施設と双方向に接続。`facility\{ワールド名}\` で整理）
- ワールドタブのツリー右クリックにレコード操作を集約（新規作成・インポート・エクスポート・JSON直接編集・複製・削除。右クリック位置から配置先を引き継ぎ）
- エリア・施設の新規作成を追加（右クリックから骨格を生成して挿入）
- クエストのモンスター・ボスの新規作成を追加（クエスト作成時に敵・ボスを新規作成でき、作成物は部品ライブラリにも保存）
- quest_event_log の圧縮を追加（ゲーム側の既知バグによるログ肥大化への緩和策。最新数ターンのみ残す）
- NPC 画像の読み込みスキップ設定を追加（画像を読み込まず動作を軽くする。プレイヤーのキャラ画像も対象）
- area / facility を削除したとき、ツリー上の下位ノード（配下の施設など）もまとめて削除するよう変更
- アイテムを新規追加すると落ちる場合がある不具合を修正、クエスト編集機能を改良

v10.2
- 自由施設（`facility_type` が `free` の施設）への暫定対応を追加（プログラムの命令列を行ごとに追加・複製・削除・並べ替え／JSON 編集、料金・報酬の編集。ツリー右クリックの「freeプログラムを作成／編集」から。施設のエクスポート／インポートでもプログラムを一緒に持ち運び、取込時に新しい施設 ID で結び直し）
- クエストのボスを新規作成すると落ちる問題を修正

v10.3
- 保存を一時ファイル経由の置き換えに変更（保存中に異常終了してもセーブ本体が壊れない）
- Ctrl+S での保存時に、入力欄から離れていない編集（`config` 等）が保存に乗らない問題を修正
- 複数行テキストの改行が消えて 1 行に潰れる問題を修正
- 未設定（null）の所有者・等級・現在地などをプルダウンで編集できるよう変更（空欄は null で保存）
- 平文 JSON として開いたファイルを保存するとき、保存形式（難読化／平文）を確認するよう変更
- 複製・削除の整合性を改善（施設・エリア・クエストの複製で ID を採番し直して接続や掲示先を張り替え、free プログラムも複製。削除時は NPC・施設・クエスト・エリアへの参照を整理し、まとめて消える施設の件数と名前を確認画面に表示）
- インポートの安全性を強化（配布 zip 内の不正なパスや極端に大きいエントリを取り込まない）
- NPC インポートを改善（プロフィール等が空だとゲームが無限ロードになる問題への補完、ゲーム側の採番と衝突しない ID の割り当て）
- バックアップのファイル名にスロット名を追加（複数ワールドを同じフォルダにまとめても世代削除が混ざらない）、同一秒に連続保存するとバックアップが欠落する問題を修正
- `namelist` の外部ファイルをカテゴリ単位で上書きするよう変更（書かなかったカテゴリは埋め込みの名前プールを維持）
- プリセット化で能力値が未設定のキャラクターは既定値で出力するよう変更
- 関係（relationship）で対象キーが重複した場合に警告するよう変更
- 予期しないエラーでツールが即終了せず、内容を表示して編集の続行・保存ができるよう変更
- 終了時にウィンドウが閉じられなくなる場合がある問題を修正
- 画面の作り直しでリソースが解放されず、長く編集していると動作が不安定になる問題を修正（レコードの選び直し、行の追加・削除など）
- 行の追加・削除ボタンを押した直後にエラーが出る問題を修正
- 画像を原寸表示したまま別のレコードを選ぶ・立ち絵を切り替えると落ちる問題を修正、原寸表示のぼやけを解消
- 画像の取込元と取込先が同じ場合に保存へ失敗する問題を修正
- 設定ファイルが壊れている場合や、画面が極端に小さい環境で起動できない問題を修正

## ライセンス
MIT License（[LICENSE](./LICENSE) を参照）。

## 謝辞
本ツールの公開を許諾してくださった Instantale 作者様（だるまと赤べこ様）に感謝します。

合わせて、ツール開発にあたり機能提案やテストにご協力くださった Discord の 狐雨月（こさめづき）様に感謝します。
